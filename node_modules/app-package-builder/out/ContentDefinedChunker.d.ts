import { FileChunks } from "builder-util-runtime/out/blockMapApi";
export declare class ContentDefinedChunker {
    computeChunks(fd: number, start: number, end: number, name: string): Promise<FileChunks>;
}
